# Bradfield Algos - Session 4 Notes

### Pre-class

* Good trick for passing `path` between recrusive calls in DFS (pass `path + [next vertex]`):

```python
traverse(path + [current_vertex], ...)
```

### In-Class

* 
